<!DOCTYPE html>
<html lang="th">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>เลือกบัญชีธนาคาร</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Sour+Gummy:ital,wght@0,100..900;1,100..900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/style/styles.css" />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
  </head>
  <body style="background-color: #8cb9bd">
    <div
      class="container m-4 text-center pt-2"
      style="
        background-color: #dae7e7;
        border-radius: 8px;
        box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.25);
        width: 95%;
        max-width: 95%;
        height: 95vh;
        max-height: 95%;
      "
    >
      <img
        src="/assets/apartment 1.svg"
        alt="apartment"
        draggable="false"
        style="position: absolute; right: 20px; bottom: 20px"
      />
      <%- include('navbar.ejs') %>
      <div class="container mt-5">
        <h2 class="text-center">กรุณาเลือกบัญชีธนาคาร</h2>
        <div class="list-group mt-4">
          <% banks.forEach(bank => { %>
          <button
            class="list-group-item list-group-item-action d-flex justify-content-between align-items-center"
            onclick="selectBank('<%= bank.bank_account_number %>')"
          >
            <img
              src="<%= bank.bank_pic %>"
              alt="<%= bank.bank_name %>"
              style="height: 40px"
            />
            <div class="ms-3">
              <strong><%= bank.bank_name %></strong> <br />
              <small>ชื่อบัญชี: <%= bank.bank_account_name %></small>
            </div>
            <i class="bx bx-chevron-right"></i>
          </button>
          <% }); %>
        </div>
      </div>
    </div>
    <script>
      const billId = "<%= bill_id %>"; // ✅ รับค่า bill_id จาก server.js

      function selectBank(bankAccountNumber) {
        window.location.href = `/pay/${bankAccountNumber}/${billId}`;
      }
    </script>
  </body>
</html>
