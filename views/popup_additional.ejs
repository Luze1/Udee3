<!-- views/partials/addExpenseModal.ejs -->
<div class="modal fade" id="addExpenseModal" tabindex="-1" aria-labelledby="addExpenseModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addExpenseModalLabel">เพิ่มรายการ (ห้อง <%= room_id %>)</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-info" role="alert">
                    ℹ️ หมายเหตุ: การเพิ่มรายการจะมีผลในบิลเดือนนี้เท่านั้น
                </div>
                <form id="expenseForm">
                    <div class="mb-3">
                        <label for="expenseDetail" class="form-label">รายละเอียด</label>
                        <input type="text" class="form-control" id="expenseDetail" required>
                    </div>
                    <div class="mb-3">
                        <label for="expenseAmount" class="form-label">จำนวนเงิน (บาท)</label>
                        <input type="number" class="form-control" id="expenseAmount" required min="0">
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
                <button type="button" class="btn btn-success" onclick="saveExpense()">บันทึก</button>
            </div>
        </div>
    </div>
</div>
