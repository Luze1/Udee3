<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เลือกห้องพัก</title>
    <link rel="stylesheet" href="/style/ReserveRoom.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- Boxicons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/style/navOwner.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body style="background-color: #d4e6e6;">
    <%- include('navOwner.html') %>
    <div id="main-content">
        <div class="container">
            <div class="header">
                <div class="logo-section">
                    <img src="/assets/logo.svg" alt="Room For Rent Logo" class="logo">
                    <div class="logo-text">เช่าห้องพัก</div>
                </div>
            </div>
    
            <h1 class="main-title">เลือกห้องพัก</h1>
            
            <div class="selection-container">
                <div class="selection-row">
                    <span class="selection-label">ชื่อ:</span>
                    <input type="text" id="tenantFirstName" placeholder="กรอกชื่อ" required>
                </div>
                <div class="selection-row">
                    <span class="selection-label">นามสกุล:</span>
                    <input type="text" id="tenantLastName" placeholder="กรอกนามสกุล" required>
                </div>
                <div class="selection-row">
                    <span class="selection-label">เลือกตึก:</span>
                    <select id="dormitorySelect" class="selection-dropdown" required>
                        <option value="">--เลือกตึก--</option>
                    </select>
                </div>
                <div class="selection-row">
                    <span class="selection-label">เลือกชั้น:</span>
                    <select id="floorSelect" class="selection-dropdown" required>
                        <option value="">--เลือกชั้น--</option>
                    </select>
                </div>
                <div class="selection-row">
                    <span class="selection-label">เลือกห้องว่าง:</span>
                    <select id="roomSelect" class="selection-dropdown" required>
                        <option value="">--เลือกห้อง--</option>
                    </select>
                </div>
                <div class="selection-row">
                    <span class="selection-label">หมายเลขบัตรประชาชน:</span>
                    <input type="text" id="user_citizen_id" placeholder="กรอกหมายเลขบัตรประชาชน" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ที่อยู่ผู้เช่า:</span>
                    <input type="text" id="user_address" placeholder="กรอกที่อยู่ผู้เช่า" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">วันที่เริ่มต้นสัญญา:</span>
                    <input type="date" id="contract_start_date" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">วันที่สิ้นสุดสัญญา:</span>
                    <input type="date" id="contract_end_date" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ระยะเวลาสัญญา (เดือน):</span>
                    <input type="number" id="contract_month" placeholder="กรอกจำนวนเดือน" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ค่าเช่า:</span>
                    <input type="number" id="rent_fee" placeholder="กรอกค่าเช่า" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ค่าประกันห้อง:</span>
                    <input type="number" id="warranty" placeholder="กรอกค่าประกันห้อง" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">เลขมิเตอร์ไฟฟ้าแรกเข้า:</span>
                    <input type="text" id="electric_meter_number" placeholder="กรอกเลขมิเตอร์ไฟฟ้า" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">เลขมิเตอร์น้ำแรกเข้า:</span>
                    <input type="text" id="water_meter_number" placeholder="กรอกเลขมิเตอร์น้ำ" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ค่าไฟฟ้าต่อหน่วย:</span>
                    <input type="number" step="0.01" id="electric_per_unit" placeholder="กรอกค่าไฟฟ้าต่อหน่วย" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">ค่าน้ำต่อหน่วย:</span>
                    <input type="number" step="0.01" id="water_per_unit" placeholder="กรอกค่าน้ำต่อหน่วย" required>
                </div>
                
                <div class="selection-row">
                    <span class="selection-label">หมายเหตุ:</span>
                    <input id="extra_condition" placeholder="กรอกหมายเหตุ">
                </div>
                <div class="selection-row">
                    <span class="selection-label">เลือกประเภทห้อง:</span>
                    <select id="roomTypeSelect" class="selection-dropdown">
                        <option value="">--เลือกประเภทห้อง--</option>
                    </select>
                </div>
            </div>
    
            <div id="rooms-container">
                <!-- รายการห้องจะแสดงที่นี่ -->
            </div>
            
            <button class="action-button" onclick="assignRoom()">เชื่อมห้องพัก</button>
        </div>
    </div>

    <script src="/ReserveRoom.js"></script>
</body>
</html>