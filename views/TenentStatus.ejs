<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สถานะห้องพัก</title> <!-- ลบ activePage ออกจาก title ถ้าไม่ต้องการ -->
    <link rel="stylesheet" href="style/TenentStatus.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <!-- Boxicons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@2.1.4/css/boxicons.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/style/navOwner.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body>
    <%- include('navOwner.html') %>
    <div class="no-bootstrap" id="main-content" style="background-color: #d4e6e6;">
        <div class="container">
            <!-- Dropdown เลือกตึก-ชั้น-ห้อง -->
            <div class="dropdown-section">
                <div class="dropdown-container">
                    <h3>เลือกตึก</h3>
                    <div class="custom-dropdown">
                        <div class="dropdown-select" onclick="toggleCustomDropdown(this)">ทุกตึก</div>
                        <div class="dropdown-list">
                        </div>
                    </div>
                </div>
                <div class="dropdown-container">
                    <h3>เลือกชั้น</h3>
                    <div class="custom-dropdown">
                        <div class="dropdown-select" onclick="toggleCustomDropdown(this)">--เลือกชั้น--</div>
                        <div class="dropdown-list">
                            <div class="dropdown-option" onclick="selectOption(this)">--เลือกชั้น--</div>
                        </div>
                    </div>
                </div>
                <div class="dropdown-container">
                    <h3>ค้นหาหมายเลขห้อง</h3>
                    <input type="text" id="roomSearch" placeholder="พิมพ์หมายเลขห้อง">
                </div>
            </div>
    
            <!-- Container สำหรับแสดงห้องแต่ละชั้น -->
            <div id="floors-container">
                <h2 class="dormitory-title">ทุกตึก</h2>
                <div class="loading-indicator">กำลังโหลดข้อมูล...</div>
            </div>
        </div>
    
        <!-- Popup รายละเอียดห้อง -->
        <div id="profilePopup" class="popup-container">
            <div class="popup">
                <div class="popup-header">
                    <h3>รายละเอียดห้อง</h3>
                </div>
                <div class="popup-body">
                    <div class="profile-info">
                        <p><strong>ห้อง:</strong> <span id="popup-room-number"></span></p>
                        <p><strong>ชื่อผู้เช่า:</strong> <span id="popup-tenant-name"></span></p>
                        <p><strong>สถานะห้อง:</strong> <span id="popup-room-status"></span></p>
                        <p><strong>เบอร์ติดต่อ:</strong> <span id="popup-phone"></span></p>
                    </div>
                </div>
                <div class="popup-footer">
                    <div class="button-row">
                        <button id="delete-tenant-btn" class="btn"></button>
                        <button class="btn btn-primary" onclick="viewContract()">ดูสัญญา</button>
                        <button class="btn btn-closed" style="background-color: lightgrey;" onclick="closePopup()">ปิด</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/TenentStatus.js"></script>
</body>
</html>